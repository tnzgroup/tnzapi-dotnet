namespace TNZAPI.NET.Samples
{
    /// <summary>
    /// This project is a reference-code library, not a runnable console app — Main is intentionally
    /// empty. Browse the sample classes under Messaging/, Addressbook/, Configuration/, and Webhooks/
    /// and call their methods from your own application. See README.md and docs/messaging.md for the
    /// full parameter reference.
    /// </summary>
    internal class Program
    {
        static void Main(string[] args)
        {
        }
    }
}